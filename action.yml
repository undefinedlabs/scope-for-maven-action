name: 'Scope for Maven'
author: 'UndefinedLabs'
description: 'Setup Scope for Maven to improve your MTTR of your broken builds and manage your flaky tests.'
inputs:
  dsn:
    description: 'Scope DSN'
    required: true
  executeTestPhase:
    description: 'Flag to indicate if test phase must be executed. default: true'
    default: 'true'
    required: true
  command:
    description: 'Command to execute test. default: mvn verify'
    default: 'mvn verify'
    required: true
runs:
  using: 'node12'
  main: 'dist/index.js'
branding:
  icon: 'check-square'
  color: 'purple'